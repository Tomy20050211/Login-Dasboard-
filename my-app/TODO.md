# TODO

- [ ] Crear componente UI reutilizable para Register/Login.
- [ ] Actualizar `RegisterForm.tsx` para usar el componente y redirigir a `/login` tras validar usuario creado.
- [ ] Actualizar `Login.tsx` para usar el componente.
- [ ] Arreglar `useRegister` para que valide y retorne `user` desde el servicio.
- [ ] Arreglar `services/register` para que retorne data y dispare errores tipados (sin alerts).
- [ ] Arreglar `useLogin` para que use `setLoading(false)` en finally y retorne `user`.
- [ ] Arreglar `services/login` para que retorne data o lance errores.
- [ ] Verificar compilación con `npm run dev` (o `npm run build`).

